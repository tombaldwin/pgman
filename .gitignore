/target
/.claude/

# candor effect-guard: commit the baselines (baseline.*), ignore the
# transient per-run output and the machine-specific local config.
.candor/report.*.json
.candor/state
.candor/last-error.log
.candor/config
.candor/.mark

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/
